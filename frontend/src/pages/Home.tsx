import Header from "../structure/Header";
import MainContainer from "../structure/MainContainer";
const Home: React.FC = () => {
  return (
    <>
      <Header />
      <MainContainer />
    </>
  );
};

export default Home;
